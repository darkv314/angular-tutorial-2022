import { IdShowDirective } from './id-show.directive';

describe('IdShowDirective', () => {
  it('should create an instance', () => {
    const directive = new IdShowDirective();
    expect(directive).toBeTruthy();
  });
});
